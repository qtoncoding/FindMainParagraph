<Page
    x:Class="FindMainParagraph_Client.SummarizePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindMainParagraph_Client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image x:Name="Original" Grid.Column="0" Grid.Row="0" Stretch="UniformToFill" Margin="5, 5, 5, 5"/>
        <Button x:Name="BrowseButton" Content ="Browse..." Grid.Column="0" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Click="BrowseButton_Click" />

        <TextBlock x:Name="Summarization" Grid.Column="1" Grid.Row="0" Margin="5, 5, 5, 5" />

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" >
            <ToggleSwitch x:Name="ParagraphSwitch" OnContent="Find main paragraph" OffContent="Original image" Margin="5, 0, 0, 0"/>
            <Button Content="Summarize" Click="Button_Click" Margin="5, 0, 0, 5"/>
        </StackPanel>
    </Grid>
</Page>
